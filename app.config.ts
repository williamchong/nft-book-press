export default defineAppConfig({
  ui: {
    colors: {
      primary: 'like-green',
      neutral: 'slate'
    },
    formField: {
      slots: {
        root: 'w-full'
      }
    },
    input: {
      slots: {
        root: 'w-full'
      }
    },
    textarea: {
      slots: {
        root: 'w-full'
      }
    }
  }
})
